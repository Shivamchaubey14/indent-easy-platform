{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "codegen": {
      "inputs": ["src/tokens.json", "scripts/**"],
      "outputs": ["dist/**"]
    },
    "build": {
      "inputs": ["src/tokens.json", "scripts/**"],
      "outputs": ["dist/**"]
    }
  }
}
