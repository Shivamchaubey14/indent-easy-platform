// Browser-safe entry point: generated TypeScript types and enum constants for the GraphQL contract.
// Server code that needs the SDL itself imports '@ie/graphql/schema'.
export * from './generated/types.js';
