# Keep the package store on D: (C: is short on space).
store-dir=D:/devtools/pnpm-store
auto-install-peers=true
# When apps/mobile (Expo) is added, revisit: SRS §63.1 suggests node-linker=hoisted for Expo + pnpm.
